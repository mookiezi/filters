# Core
pandas>=2.2.0
polars>=0.20.0
pyarrow>=15.0.0
rich>=13.7.0
psutil>=5.9.0
emoji>=2.10.0

# NLP / tokenization
transformers>=4.41.0

# Optional (some systems split these; transformers will usually pull tokenizers)
tokenizers>=0.19.0